[package]
name = "better_enums_derive"
version = "0.1.0"
edition = "2024"
description = "This crate is not intended to use directly, use better_enums instead"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
proc-macro-crate = "3.5.0"